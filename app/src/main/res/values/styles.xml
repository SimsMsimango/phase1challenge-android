<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyCustomButton" parent="@android:style/Widget.Button">
        <item name="android:backgroundTint">@android:color/holo_orange_light</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Display1</item>
        <item name="android:background">@drawable/button_corners</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
    </style>

    <style name="CustomActivityATheme" parent="AppTheme.NoActionBar">
        <!--<item name="android:textViewStyle">@style/TextAppearance.AppCompat.Display2</item>-->
        <item name="buttonStyle">@style/MyCustomButton</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CustomActivityCTheme" parent="AppTheme.NoActionBar">
        <item name="android:textViewStyle">@android:style/TextAppearance.DeviceDefault.Large</item>
    </style>

</resources>
